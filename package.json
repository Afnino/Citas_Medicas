{
  "name": "demo-node",
  "private": true,
  "scripts": {
    "dev": "concurrently -n servidor,cliente -c blue,green \"npm run dev --prefix server\" \"npm run dev --prefix client\"",
    "dev:servidor": "npm run dev --prefix server",
    "dev:cliente": "npm run dev --prefix client"
  },
  "devDependencies": {
    "concurrently": "^9.2.1"
  }
}
